<UserControl x:Class="MI50FanControl.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:loc="clr-namespace:MI50FanControl.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="850">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24,20,24,24">

            <!-- Title -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="{loc:Loc SettingsTitle}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>

            <!-- 1. Detected Hardware Info Card -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="{loc:Loc HardwareInfoSection}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>

                    <!-- Motherboard -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{loc:Loc Motherboard}" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Motherboard}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </Grid>

                    <!-- SuperIO Chip -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{loc:Loc SuperIoChip}" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding SuperIoChip}" FontWeight="SemiBold" Foreground="#38BDF8"/>
                    </Grid>

                    <!-- GPU Model -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{loc:Loc GpuModel}" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding GpuModel}" FontWeight="SemiBold" Foreground="{StaticResource AccentCyanBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 2. Language & Localization Card -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="{loc:Loc LanguageSection}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="260"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{loc:Loc SelectLanguage}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>

                        <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableLanguages}" SelectedItem="{Binding SelectedLanguage}" DisplayMemberPath="DisplayName"
                                  Style="{StaticResource ModernComboBox}" VerticalAlignment="Center"/>

                        <Button Grid.Column="2" Command="{Binding OpenLangFolderCommand}" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left" Margin="16,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📁 "/>
                                <TextBlock Text="{loc:Loc OpenLangFolder}"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 3. System Behavior Card -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="{loc:Loc SystemBehaviorSection}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>

                    <CheckBox IsChecked="{Binding StartWithWindows}" Style="{StaticResource ModernCheckBox}" Margin="0,0,0,10">
                        <TextBlock Text="{loc:Loc StartWithWindows}"/>
                    </CheckBox>

                    <CheckBox IsChecked="{Binding MinimizeToTrayClose}" Style="{StaticResource ModernCheckBox}" Margin="0,0,0,10">
                        <TextBlock Text="{loc:Loc MinimizeToTrayClose}"/>
                    </CheckBox>

                    <CheckBox IsChecked="{Binding MinimizeToTrayMin}" Style="{StaticResource ModernCheckBox}">
                        <TextBlock Text="{loc:Loc MinimizeToTrayMin}"/>
                    </CheckBox>
                </StackPanel>
            </Border>

            <!-- 4. Emergency Overheat Threshold Card -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="{loc:Loc EmergencyProtection}" FontSize="14" FontWeight="Bold" Foreground="#FCA5A5" Margin="0,0,0,4"/>
                    <TextBlock Text="{loc:Loc EmergencyDesc}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,14"/>

                    <!-- Slider Row -->
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="240"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="65"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{loc:Loc EmergencyThreshold}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        <Slider Grid.Column="1" Minimum="70" Maximum="105" Value="{Binding EmergencyThreshold}" Style="{StaticResource ModernSlider}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding EmergencyThresholdText}" Foreground="#F87171" FontWeight="Bold" FontSize="14" TextAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Save & Apply Bar -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding SaveStatusMessage}" Foreground="#4ADE80" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center"/>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Command="{Binding ResetEmergencyDefaultCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="↺ "/>
                                    <TextBlock Text="{loc:Loc DefaultEmergencyBtn}"/>
                                </StackPanel>
                            </Button>

                            <Button Command="{Binding SaveEmergencyCommand}" Style="{StaticResource PrimaryButton}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💾 "/>
                                    <TextBlock Text="{loc:Loc SaveAndApply}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 5. Factory Defaults Reset Card -->
            <Border Style="{StaticResource ModernCard}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{loc:Loc ResetDefaults}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="{loc:Loc ResetDefaultsDesc}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,16,0" TextWrapping="Wrap"/>
                        </StackPanel>

                        <Button Grid.Column="1" Command="{Binding ResetAllDefaultsCommand}" Style="{StaticResource DangerButton}" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="↺ "/>
                                <TextBlock Text="{loc:Loc ResetDefaultsBtn}"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
