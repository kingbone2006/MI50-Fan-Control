<UserControl x:Class="MI50FanControl.Views.DevLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:loc="clr-namespace:MI50FanControl.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / Title -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="🛠️ Nhật Ký Gỡ Lỗi &amp; Chế Độ Nhà Phát Triển" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Xem chi tiết giao tiếp phần cứng Ring0, AMD ADL Telemetry, SuperIO PWM writes và kiểm tra phần cứng trực tiếp." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="📋 Sao Chép" Command="{Binding CopyLogsCommand}" Style="{StaticResource PrimaryButton}" Padding="12,6" Margin="0,0,8,0"/>
                <Button Content="💾 Lưu File Log" Command="{Binding SaveLogFileCommand}" Style="{StaticResource PrimaryButton}" Padding="12,6" Margin="0,0,8,0"/>
                <Button Content="🧹 Xóa Log" Command="{Binding ClearLogsCommand}" Style="{StaticResource PrimaryButton}" Padding="12,6"/>
            </StackPanel>
        </Grid>

        <!-- Toolbar & Direct Hardware Actions -->
        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Filter Selector -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Bộ lọc:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Width="130" SelectedValue="{Binding SelectedFilter, Mode=TwoWay}" SelectedValuePath="Content" VerticalAlignment="Center">
                        <ComboBoxItem Content="All" IsSelected="True"/>
                        <ComboBoxItem Content="Hardware"/>
                        <ComboBoxItem Content="AMD ADL"/>
                        <ComboBoxItem Content="PWM"/>
                        <ComboBoxItem Content="Errors"/>
                    </ComboBox>
                </StackPanel>

                <!-- Direct Hardware Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Thử phần cứng trực tiếp:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button Content="🔄 Quét Lại" Command="{Binding RescanHardwareCommand}" Style="{StaticResource PrimaryButton}" Padding="10,4" Margin="0,0,6,0"/>
                    <Button Content="Ép 100% PWM" Command="{Binding Force100Command}" Background="#DC2626" Foreground="#FFFFFF" FontWeight="Bold" Padding="10,4" Margin="0,0,6,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Content="Ép 50% PWM" Command="{Binding Force50Command}" Background="#F59E0B" Foreground="#0D1117" FontWeight="Bold" Padding="10,4" Margin="0,0,6,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Content="Khôi Phục BIOS" Command="{Binding RestoreBiosCommand}" Background="#10B981" Foreground="#FFFFFF" FontWeight="Bold" Padding="10,4">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Log Terminal View -->
        <Border Grid.Row="2" Background="#0A0D12" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" Padding="8">
            <ListBox x:Name="LogListBox" ItemsSource="{Binding DisplayedEntries}" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Timestamp -->
                            <TextBlock Grid.Column="0" Text="{Binding TimeFormatted}" FontFamily="Consolas, Cascadia Mono, monospace" FontSize="11" Foreground="#6E7681" Margin="0,0,8,0"/>

                            <!-- Badge -->
                            <TextBlock Grid.Column="1" Text="{Binding LevelBadge}" FontFamily="Consolas, Cascadia Mono, monospace" FontSize="11" FontWeight="Bold" Foreground="{Binding Level, Converter={StaticResource LogLevelToBrush}}" Margin="0,0,8,0"/>

                            <!-- Category -->
                            <TextBlock Grid.Column="2" Text="{Binding Category, StringFormat='[{0}]'}" FontFamily="Consolas, Cascadia Mono, monospace" FontSize="11" FontWeight="SemiBold" Foreground="#58A6FF" Margin="0,0,8,0"/>

                            <!-- Message -->
                            <TextBlock Grid.Column="3" Text="{Binding Message}" FontFamily="Consolas, Cascadia Mono, monospace" FontSize="11" Foreground="{Binding Level, Converter={StaticResource LogLevelToBrush}}" TextWrapping="Wrap"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
